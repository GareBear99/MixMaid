name: Build & Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      tag:
        description: 'Release tag (e.g. v0.9.0-beta.1)'
        required: true

permissions:
  contents: write

jobs:
  build-macos:
    runs-on: macos-14
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Clone JUCE 8.0.0
        run: git clone --depth 1 --branch 8.0.0 https://github.com/juce-framework/JUCE.git JUCE

      - name: Install CMake
        run: brew install cmake

      - name: Configure
        run: |
          cmake -S MixMaid_v9 -B build \
            -DJUCE_PATH=$PWD/JUCE \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_OSX_ARCHITECTURES="arm64;x86_64" \
            -DCMAKE_OSX_DEPLOYMENT_TARGET=11.0 \
            -Wno-dev

      - name: Build VST3 + AU + Standalone
        run: |
          cmake --build build --config Release --target MixMaid_VST3 -j
          cmake --build build --config Release --target MixMaid_AU -j
          cmake --build build --config Release --target MixMaid_Standalone -j

      - name: Verify universal binaries
        run: |
          echo "=== VST3 ==="
          file build/MixMaid_artefacts/Release/VST3/MixMaid.vst3/Contents/MacOS/MixMaid
          lipo -info build/MixMaid_artefacts/Release/VST3/MixMaid.vst3/Contents/MacOS/MixMaid
          echo "=== AU ==="
          file build/MixMaid_artefacts/Release/AU/MixMaid.component/Contents/MacOS/MixMaid
          lipo -info build/MixMaid_artefacts/Release/AU/MixMaid.component/Contents/MacOS/MixMaid

      - name: Package zips
        run: |
          mkdir -p dist
          (cd build/MixMaid_artefacts/Release/VST3 && zip -r ../../../../dist/MixMaid-VST3-macOS-universal.zip MixMaid.vst3)
          (cd build/MixMaid_artefacts/Release/AU && zip -r ../../../../dist/MixMaid-AU-macOS-universal.zip MixMaid.component)

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: MixMaid-macos-universal
          path: dist/*.zip

      - name: Attach to release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v1
        with:
          files: dist/*.zip
          prerelease: ${{ contains(github.ref, '-beta') || contains(github.ref, '-alpha') || contains(github.ref, '-rc') }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
